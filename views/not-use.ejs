<!-- <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-3">
    <div class="bg-white p-6 rounded-lg shadow-lg w-[300px] h-[201px] mt-0 ml-[19px] mb-0 mr-0 bg-[url('/assets/box.jpg')] bg-no-repeat">
      <div class="card-header">
        <h2 class="card-title text-2xl font-bold mb-4">Services</h2>
      </div>
      <div class="card-content">
        <ul class="space-y-2">
          <% data.services.forEach(function(service) { %>
            <li class="flex items-center space-x-2">
              <span class="h-1 w-1 rounded-full bg-blue-500"></span>
              <a href="#" class="text-sm hover:text-blue-500"><%= service %></a>
            </li>
          <% }) %>
        </ul>
        <a href="/services" class="text-sm text-blue-500 hover:underline mt-4 block">More</a>
      </div>
    </div>

    <div class="card bg-white p-6 rounded-lg shadow-lg">
      <div class="card-header">
        <h2 class="card-title text-2xl font-bold mb-4">Solutions</h2>
      </div>
      <div class="card-content">
        <ul class="space-y-2">
          <% data.solutions.forEach(function(solution) { %>
            <li class="flex items-center space-x-2">
              <span class="h-1 w-1 rounded-full bg-blue-500"></span>
              <a href="#" class="text-sm hover:text-blue-500"><%= solution %></a>
            </li>
          <% }) %>
        </ul>
        <a href="/solutions" class="text-sm text-blue-500 hover:underline mt-4 block">More</a>
      </div>
    </div>

    <div class="card bg-white p-6 rounded-lg shadow-lg">
      <div class="card-header">
        <h2 class="card-title text-2xl font-bold mb-4">Training</h2>
      </div>
      <div class="card-content">
        <p class="text-sm text-gray-600 mb-4">
          We have in-depth training program for various corporations and Technical or Non-Technical Professionals.
        </p>
        <ul class="space-y-2">
          <% data.training.forEach(function(training) { %>
            <li class="flex items-center space-x-2">
              <span class="h-1 w-1 rounded-full bg-blue-500"></span>
              <a href="#" class="text-sm hover:text-blue-500"><%= training %></a>
            </li>
          <% }) %>
        </ul>
        <a href="/training" class="text-sm text-blue-500 hover:underline mt-4 block">More</a>
      </div>
    </div>
  </div> -->

  <div class="bg-white rounded-lg shadow-sm p-6 hover:shadow-md transition-shadow border-l-4 border-primary">
    <h3 class="text-xl font-semibold text-primary-dark mb-4">
      <%= service.title %>
    </h3>
    <ul class="space-y-2">
      <% service.items.forEach(item => { %>
        <li class="text-gray-600 hover:text-accent transition-colors">
          <a href="<%= item.link %>"><%= item.name %></a>
        </li>
      <% }) %>
    </ul>
    <a href="/services" class="text-secondary hover:text-accent transition-colors mt-4 inline-block font-semibold">
      More
    </a>
  </div>
.....................................







<nav class="w-full border-b sticky bg-blue-900 top-0 z-40">
  <div class="w-full">
      <!-- Logo -->
      <div id="logo">
          <p class="text-2xl font-bold text-white text-center py-2">Mabridge</p>
      </div>

      <div id="navbarItems" class="w-full md:w-[80%] md:mx-auto pb-1 md:pb-0">
          <div class="flex items-center justify-end md:justify-normal h-full mr-3 md:mx-0">
              <!-- Desktop Navigation -->
              <div class="hidden md:flex items-center justify-between">
                  <% navItems.forEach(function(item) { %>
                      <a id="navbarItem" href="<%= item.href %>" class="flex items-center h-full md:px-4 lg:px-6 py-3 text-white hover:text-blue-300 hover:bg-blue-700 transition-colors">
                          <%= item.name %>
                      </a>
                  <% }) %>
              </div>

              <!-- Mobile menu button -->
              <div class="md:hidden">
                  <button id="menu-button" class="inline-flex items-center justify-center p-1 rounded-md text-white border border-gray-100 hover:bg-blue-600 focus:outline-none">
                      <svg id="menu-icon" class="h-7 w-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
                      </svg>
                  </button>
              </div>
          </div>
      </div>
  </div>

  <!-- Mobile Navigation -->
  <div id="nav-menu" class="fixed left-0 top-0 transform -translate-x-full transition-transform duration-200 ease-in-out z-50 w-64 h-full bg-gray-50">
      <div class="flex flex-col">
          <% navItems.forEach(function(item, index) { %>
              <a href="<%= item.href %>" class="block text-gray-900 hover:bg-blue-600 transition-colors px-4 py-3 border-b border-gray-300 <%= (index === navItems.length - 1) ? 'last-item' : '' %>">
                  <%= item.name %>
              </a>
          <% }) %>
      </div>
  </div>

  <!-- Overlay for mobile -->
  <div id="nav-overlay" class="fixed inset-0 bg-black opacity-70 z-40 hidden md:hidden"></div>
</nav>
